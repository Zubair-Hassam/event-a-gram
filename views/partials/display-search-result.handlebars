<div class="card column mx-auto mt-5 is-two-thirds-tablet is-one-third-desktop is-one-quarter-widescreen"> {{!--  is-two-thirds-tablet --}}
     <header class="card-header">
        <h4 class="card-header-title is-centered"> {{title}}</h4>
    </header>
    
    <div class="card-image">
        <figure class="image mx-auto">
            <img src="/images/{{theme.theme_description}}.png" alt="{{theme.theme_description}}">
        </figure>
    </div>
   
    <div class="card-content">
        <div class="content "><h5>
            The host of this event is {{user.firstname}} {{user.lastname}}.
            <hr>
            The host has labeled the event as a {{theme.theme_description}} event.
            <hr>
            The event is scheduled to happen on {{format_date startdate}}.
            <hr>
            {{#if ispublic}}
            It's open to the public -- consider swinging by!
            <hr>
            {{else}}
            The host marked this event as private. 
            <hr>
            {{/if}}
            {{#if party.isover18}}
            The age minimum is 18 years old.
            <hr>
            {{else}}
            The event is open to minors.
            <hr>
            {{/if}}
           
            Contact the Host for additional Event Details and Bookings</h5>
            <br>
               <br>
           <button class="emailbtn"> <a href="mailto:{{user.email}}">{{user.email}}</a>
        </div></button>

 
               
                
                <br>
               
         <div class="card-footer is-flex-direction-column">
        <h5 class="color-white"><a href="/dashboard" class="card-footer-item" id="goToDashboardBtn">Go back to my Dashboard</a></h5>
    </div>
     </div>
      </div>
   

   